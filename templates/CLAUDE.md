@AGENTS.md

<!--
  Claude Code reads CLAUDE.md and not AGENTS.md, so the import above is what
  lets both this and any other agent read one set of instructions instead of
  two that drift apart. Keep the content in AGENTS.md; keep this file thin.

  Below the import, add only things that are specific to Claude Code — plan
  mode for particular paths, skills to prefer, tools to avoid. If you have
  nothing Claude-specific to say, leave this file as the single import line.
  That is a perfectly good CLAUDE.md.
-->
